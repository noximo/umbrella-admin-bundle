{% extends "@UmbrellaAdmin/email/layout.html.twig" %}

{% trans_default_domain 'UmbrellaAdmin' %}
{% block content %}
    {{ 'password_resetting.email.body' | trans({
        '%name%': user.getFullName(),
        '%app_name%': umbrella_admin.appName(),
        '%reset_url%': url('umbrella_admin_security_passwordreset', {token: token})
    }) | raw }}
{% endblock %}